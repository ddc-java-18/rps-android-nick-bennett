/**
 * Repository and service classes providing access (at least potentially) to device &amp; external
 * services, preferences, the app's persistent data store, and the domain model.
 */
package edu.cnm.deepdive.rps.service;